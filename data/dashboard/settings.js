settings = {
    pan_mode : 'xy',   // Enable panning on the x and/or y axis; options are x, y, xy       Default = xy
    pan_speed : 0.3, // Adjust pan speed (lower = slower).                                  Default = 0.3
    pan_threshold: 100, // Minimum distance to start panning (useful for touch devices)     Default = 100
    zoom_mode: 'xy', //enable zooming on x, y or both; options x, y, xy                     Default = xy
    zoom_wheel_speed: 0.1, //Speed of zooming in with mousewheel                            Default = 0.1
    zoom_pinch_speed: 0.2  //Speed of zooming using touchpad                                Default = 0.2
}